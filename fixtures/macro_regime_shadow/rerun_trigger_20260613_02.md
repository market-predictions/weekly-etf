# Rerun trigger

Requested after confirming the old macro-audit fixture uses:

```text
summary.max_staleness_days = max(observed staleness)
```

This rerun should pick up the latest macro audit validator compatibility fix.

Expected workflow:

```text
Validate ETF macro regime shadow
```
