# Rerun trigger

Requested after cleaning the macro-regime-shadow evidence commit working tree before rebase.

Expected workflow:

```text
Validate ETF macro regime shadow
```

Expected result:

```text
validation passes
evidence commit succeeds
latest_macro_regime_shadow_validation.json updates
```
