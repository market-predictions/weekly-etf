# Rerun trigger

Requested after hardening the macro-regime-shadow evidence commit step against remote-ahead push races.

Expected workflow:

```text
Validate ETF macro regime shadow
```

Expected result:

```text
validation passes and evidence commit succeeds
```
