# Rerun trigger

Requested by user to rerun the Validate ETF macro regime shadow workflow after the macro audit staleness summary validator fix.

Expected workflow:

```text
Validate ETF macro regime shadow
```

Expected result:

```text
workflow_success
```
