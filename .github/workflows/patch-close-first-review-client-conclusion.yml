name: Patch close-first review client conclusion

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/patch-close-first-review-client-conclusion.yml"
      - "tools/patch_close_first_review_client_conclusion.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Apply client-language refinement
        run: |
          set -euo pipefail
          python tools/patch_close_first_review_client_conclusion.py
          python -m py_compile \
            runtime/build_portfolio_close_first_review.py \
            tests/test_portfolio_close_first_review.py \
            tools/validate_portfolio_close_first_review.py
          rm -f tools/patch_close_first_review_client_conclusion.py
          rm -f .github/workflows/patch-close-first-review-client-conclusion.yml

      - name: Commit refinement and self-cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runtime/build_portfolio_close_first_review.py tests/test_portfolio_close_first_review.py tools/validate_portfolio_close_first_review.py
          git add -u tools/patch_close_first_review_client_conclusion.py .github/workflows/patch-close-first-review-client-conclusion.yml
          git commit -m "Refine close-first review client conclusion [skip ci]"
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
