name: Patch fresh report delivery request authority

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/patch-fresh-report-delivery-request-authority.yml"
      - "tools/patch_fresh_report_delivery_request_authority.py"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - name: Check out implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/fresh-report-delivery-authority-pr
          fetch-depth: 0

      - name: Apply deterministic patch
        run: python tools/patch_fresh_report_delivery_request_authority.py

      - name: Keep workflow changes outside bot commit
        run: |
          git checkout HEAD -- \
            .github/workflows/patch-fresh-report-delivery-request-authority.yml \
            tools/patch_fresh_report_delivery_request_authority.py
          rm -f .github/workflows/validate-report-request-execution-authority.yml

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install focused test dependencies
        run: python -m pip install pytest pyyaml

      - name: Compile and validate authority patch
        id: authority_tests
        continue-on-error: true
        run: |
          set -o pipefail
          {
            python -m py_compile \
              runtime/model_execution_guarded_auto.py \
              tests/test_model_execution_request_authority.py
            pytest -q \
              tests/test_model_execution_request_authority.py \
              tests/test_model_no_trade_gate.py
          } 2>&1 | tee /tmp/report-authority-tests.txt

      - name: Upload authority test diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: report-authority-tests-${{ github.run_id }}
          path: /tmp/report-authority-tests.txt
          if-no-files-found: error

      - name: Enforce authority tests
        if: steps.authority_tests.outcome != 'success'
        run: exit 1

      - name: Commit validated code and request
        if: steps.authority_tests.outcome == 'success'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            runtime/model_execution_guarded_auto.py \
            tests/test_model_execution_request_authority.py \
            control/run_queue/README.md \
            control/run_queue/weekly_etf_report_request_20260718_125324.md \
            control/work_packages/WP_FRESH_REPORT_DELIVERY_REQUEST_AUTHORITY_20260718.md \
            control/work_package_claims/WP_FRESH_REPORT_DELIVERY_REQUEST_AUTHORITY_20260718.md
          git commit -m "Add report-request execution authority gate" || exit 0
          git push origin HEAD:agent/fresh-report-delivery-authority-pr
